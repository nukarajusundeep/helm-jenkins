apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: nginx-jenkins
  namespace: jenkins
spec:
  rules:
    - host: nukaraju.dev
      http:
        paths:
          - backend:
              serviceName: jenkins-ci
              servicePort: 8080
            path: /