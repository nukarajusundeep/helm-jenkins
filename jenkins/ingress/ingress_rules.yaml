apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: nginx-jenkins
spec:
  rules:
    - host:
      http:
        paths:
          - backend:
              serviceName: jenkins-ci
              servicePort: 8080
            path: /